=========================================

C++ Seminar 

Informatik // IMMD  Institut f. Mathematische Maschinen

Peter Loos  /  Yavuz

peter.loos@gmx.de
-----------------

3 Tage C++ 

Java, C#, JavaScript, Python

=========================================

Unterlagen:

https://github.com/pelocpp

https://github.com/pelocpp/cpp_introduction

=========================================

C/ C++ (?) 
Was sind meine Erwartungen
Was soll es sein ...


Focus: Pointer, Referenzen, STL, 

=========================================

a) Github

b) Visual Studio (NICHT: Code) C++ 

   Einfach ....  Vorteil: Da ist (Installation) C/C++ dabei ...


=========================================

Visual Studio:

Visual Studio Community 2026  / 2022

=========================================

Erste Schritte:

a) Right-Click auf der Datei: Menü Compile ==> Grammatik / Syntax

b) Right-Click auf dem Projekt: Build / Erstellen ==> Maschinenprogramm (.exe Datei)

====================

Wie findet der Build statt?

====================

Unser erstes C++ Programm:

Klasse und Objekt:

a) Klassen sind die Weiterführung von Strukturen aus C.

b) Variablen von Klassen nennt man Objekt / Instanz

Umsetzung:  3 Dateien

i)    Header-Datei (Dateiendung .h):

ii)   Implementierungs-Datei (Dateiendung .cpp):
      Realisierung der Methoden der Klasse

iii)  Hauptprogramm / Anwendung / Benutzer der Klasse Time

==============================================

#include <iostream>  <=== Standard Dateien

#include "Time.h"    <=== unsere eigenen Dateien / benutzer-definierte Dateien / user-defined File

==============================================

Ausgabe:

Klassisches Vorgehen:  std::cout

Modernes Vorgehen:     std::println  // ähnlich zu C:  printf

==============================================

Debuggen:

Für einfache Schritte sehr einfach:

a) Haltepunkt / Break Point: Mit der Maus

b) Bis zum Haltepunkt laufen: Grüner "dicker" Pfeil / Programm starten

c) Rotes Rechteck: Stoppen / Programm beenden

d) Einzelschritt / Eine Zeile ausführen:    F10 oder "Pfeil nach rechts"

e) Einzelschritt für eine Methode / Unterprogramm:

   F11 oder "Pfeil nach unten"


=====================================================================

Zugriffsklassen:

"Wie" kann man auf die Daten und Methoden eines Objekts zugreifen:

i) Nicht immer :)

a) public versus private

b) protected: Spielt nur bei der Vererbung eine Rolle

Welche gibt es: public, private, protected

=====================================================================

Initialisierung von Objekten: Konstruktoren

Schwachstelle:

    Time now;     // ein Objekt

Objekt now hat so keine initialien Werten. Das will man nicht :)

=======================================================================

Was ist die Element-Initialisierungsliste (Member Initializer List) ???

A) Sie wurde im Umfeld der Vererbung eingeführt // sie ist dort wichtig.

B) Man kann sie aber auch ohne Vererbung einsetzen:

   Sie stellt dann  eine gewisse abgekürzte Schreibweise zur Verfügung.

=====================================================================

   ACHTUNG: Stolperfalle bei C++ und Konstruktoren

    Time now();

In dieser Zeile wird NICHT (!!!!!!!!!!!!!!!!) 
der Konstruktor OHNE Parameter aufgerufen !!!

Was findet dann statt:

Es wird eine freie Funktion now deklariert, keine Parameter (), mit Rückgabetyp Time.

Möchte man den Konstruktor OHNE Parameter aufrufen: 
Dann müssen die Klammern weg !!!!!!!!!!!!

==========================================

static:   Modulkonzept für Arme

==========================================

Zeiger, Adressen, Referenzen in C/C++
-------------------------------------

Unter historischer Berücksichtigung:

A) Was gibt es in C?

   Adressen (Address) und Zeiger (Pointer).

   int*   : Beschreibt einen Datentyp: Es ist ein Adress Datentyp.

   Beispiel:  0x00000084f3eff 864

   Was liegt hinter einer Adresse // Wieviele Daten liegen hinter einer Adresse? 1 Byte

   Ein int belegt 4 Bytes:

   0x00000084f3eff 864 beschreibt das ERSTE Byte einer int-Variablen // Anfangsadresse
   0x00000084f3eff 865
   0x00000084f3eff 866
   0x00000084f3eff 867

   Adresse des nächsten, frei verfügbaren Speichers:
   0x00000084f3eff 868

   Nebenbemerkung: Was sind das für Bytes zwischen 2 Variablen:

	0x0000006E796FF318  cc cc cc cc  ÌÌÌÌ
	0x0000006E796FF31C  cc cc cc cc  ÌÌÌÌ
	0x0000006E796FF320  cc cc cc cc  ÌÌÌÌ
	0x0000006E796FF324  cc cc cc cc  ÌÌÌÌ
	0x0000006E796FF328  cc cc cc cc  ÌÌÌÌ
	0x0000006E796FF32C  cc cc cc cc  ÌÌÌÌ
	0x0000006E796FF330  cc cc cc cc  ÌÌÌÌ
	0x0000006E796FF334  cc cc cc cc  ÌÌÌÌ

	Das sind zum Zwecke des Testens / Fehlersuchens so genannte "Schattebytes" / Shadow Bytes:
	Whyyyyyyyyy: Wenn einer dieser cc-Werte überschrieben wird, dann bricht der Debugger die Ausführung des Programms ab !!!


	Wie kann man mit Adressen arbeiten:

	ZWEI Operatoren:

	a) &:  Bilde eine Adresse

	b) *:  Indirekter Zugriff auf den Speicher hinter der Adresse (lesend oder schreibend)

    // -----------------------------------
    // Zeigerarithmetik

    // a) Wert eines Zeigers verändern / inkrementieren
    ip++;

	Beispiel:

	Vorher:

	ip:  0x0000006E796FF3 18

	Was steht nach ip++ für ein Wert in ip ????

	Antwort: 0x0000006E796FF3 19

	Idee / Konzeption: Mit ++ auf einer Adresse will man im Speicher zur nächsten FREIEN Stelle gehen.

	0x0000006E796FF3 18
	0x0000006E796FF3 19
	0x0000006E796FF3 1A
	0x0000006E796FF3 1B

	0x0000006E796FF3 1C  erste freie Stelle

===========================================================================

Spielen Adressen in C++ auch eine Rolle:

Ja, aber an einer anderen Stelle: Dynamische Speicherverwaltung.

===========================================================================
===========================================================================

B) Was gibt es in C++?

Bemerkung: C ist in C++ enthalten !

===> Referenzen.

Vorbemerkung:

Ist doch ähnlich zu Pointer / Adressen. Whyyyy? 
Man wollte C++ an manchen Stellen einfacher lesbar machen.

=============================================================

C++:  Ist streng typisiert

int n = 123;

n = "ABC";   // NIEEEEEEEEEEEEEEEEEEEEE // ERROR !!!!!!!!!!!!!!!!!!!!!



Python:   Typen werden zur Laufzeit den Bezeichner gebildet.

n = 123    // hmm, n ist ein int // n zeigt auf ein int ...

n = "ABC"  // hmmm, JETZT zeigt n auf eine Zeichenkette

=============================================================

Wo werden Referenzen in C++ verwendet ????????????????


Parameter-Übergabe :  Welche Formen ....

Stack / Heap

Stack: Ist für Parameter zuständig
Heap:  Ist für dynamische Daten zuständig

a) Wie sieht die Parameter-Übergabe im Detail aus ???

   Da gibt es mehrere Optionen:

   i) Call-by-Value       // Call-by-Copy

   ii) Call-by-Address    // Das ist ein C-Sprachmittel (damit auch in C++ verfügbar)

   ii) Call-by-Reference  // Das ist ein C++-Sprachmittel (nicht in C verfügbar)

         Hinter den Kulissen: Quasi dasselbe wie Call-by-Address
		 Schreibweise / Programmierung: Erheblich einfacher / besser lesbar

   Voreinstellung (Default) ist Call-by-Copy (Call-by-Value)


b) Wo liegen lokale Variablen im Speicher ???

   i) Auf dem Stack
   ii) Dieser wächst pro Unterprogramm-Aufruf in eine Richtung
   iii) Nach Rückkehr verkleinert es sich in der anderen Richtung
   iv) Wird durch Register wie den SP (Stack Pointer) verwaltet.

=================================================================

Weiter am Dienstag morgen:

a) Anwendung der Parameterübergabetechnik auf Methoden einer Klasse

b) const:  Kommt noch

....


=================================================================

Überladen von Operatoren

    if (now.equal(than) == true) {  

Hmmm, wäre nicht folgende Zeile besser lesbar:

int a, b;
     
	if (a == b) { ...

    if (now == than) {  

Gehr prinzipiell schon, aber wir müssen "den Operator == überladen"

Wie geht das ???

Wie das Erstellen / Schreiben einer normalen Methode:

   Eigenschaft: Welchen Namen hat diese Methode:  operator ==

Besonderfall:

Wenn der Operator == realisiert wird, wird der Operator != automatisch vom Übersetzer erstellt.

=========================================================================

Nochmal Parameterübergabe:

a) Auch Objekte können an Methoden / freie Funktionen übergeben werden (trivial)

b) ACHTUNG: Call-by-Copy ist Voreinstellung

c) Dies will man in den meisten Fällen vermeiden

d) Lösung:  Call-by-Reference

e) Theoretisch: Call-by-Address / mit Pointer: Unüblich, weil die Syntax von Referenzen einfacher ist.

Fazit: Go-for  Call-by-Reference, wegen der Performanz ( das Erzeugen von Kopien kostet überfüssige Zeit )

--------------------------

Eine Ergänzung:

i) Mit Referenzen bekommt man den Zugang zu einem Originalobjekt (Referenz = Alias)

ii) Beim lesenden Zugriff sollte man das Originalobjekt schützen: Mit einer const - Referenz !!!!!!!!!!!!!

    const beim Parameter

iii) Man kann auch Methoden einer Klasse, die ihre eigenen Membervariablen LESEND verwendet, 
     als const definieren ====> ein Schreiben von Membervariablen geht dann nicht. 

	 const HINTER der Methode

Fazit: Go-for const, um sichereren Quellcode zu erstellen.


=========================================================

Topic: "Freundschaft"  // friendship

Beispiel:

bool operator== (const Time& lhs, const Time& rhs)
{
    if (lhs.m_hours == rhs.m_hours && lhs.m_minutes == rhs.m_minutes && lhs.m_seconds == rhs.m_seconds) {
        return true;
    }
    else {
        return false;
    }
}

A) Geht so nicht, weil operator == ist eine freie Funktion: Diese hat keine Möglichkeit,
auf private Member Variablen zuzugreifen.

B) Hmmm, man könnte die Meinung haben, dass - in diesem Fall - der Operator == , auf 2 Time Objekt angewendet ,
   irgendwie doch zur Klasse Time gehören sollte:

   Das geht mit dem Schlüsselwort 'friend'

ACHTUNG: Mit friend werden private Member zu public Member.

Das sollte man sich genau überlegen: Ich würde friend nur in Ausnahmefällen einsetzen.

===================================================================


Aufgabe Wallet:

 std::cout << wallet << std::endl;

 NEIN:

 Statt dessen:

 wallet.print();
 wallet.add(5);   // 5 Euros

 wallet.add(5, 25);   // 5 Euros und 25 Cent

 bool success = wallet.sub(3);    // 3 Euros entnehmen

 // Geht nur , wenn genügend Geld im Geldbeutel ist 

 == Konstruktoren  // Am Anfang ist da ein Euro drin (( leer: 0

 == Methoden

 == Operatoren:   operator ==

 ==               operator <    // in welchem von 2 Geldbeuteln ist mehr drin

      Die Operatoren IN oder AUSSERHALB der Klasse implementieren.

========================================

Vererbung // Inheritance



// Eine Klasse für Rechtecke

class Rectangle

// Brauchen noch eine solche Klasse: Rechtecke mit Farbe

Wann sollte / kann / darf man das Feature der Vererbung sinnvoll anwenden ???

Betrachtung / Entwurf / Design von Software:

a) Klasse Rectangle
b) Klasse ColoredRectangle

Aufgabe:  Umgangssprachlichen Satz, in dem die zwei Wörter Rectangle und ColoredRectangle vorkommen.

i) Ein ColoredRectangle ist eine Variante von einem Rectangle.

ii) Ein ColoredRectangle ist ein Rectangle.

Beziehungen zwischen Klassen // Relationsship:

A) Beziehung "ist-ein"  // "is-a" Relationsship

   Die  "ist-ein" Beziehung muss erfüllt sein, wenn ich die Vererbung anwenden möchte.

=============================================================================

Probe:

a) Klasse Point   // 2-dim Ebene
b) Klasse Line    // 2-dim Ebene

Satz:

a) Zwei Punkte sind eine Linie.   // hmmmmmmmmmm

b) Eine Linie kann aus 2 Punkten bestehen // hat zwei Punkte // Anfangs- und Ende-Punkt

c) Ein Punkt ist eine Linie: Wahr oder falsch: Falsch
   Zwei Punkte sind eine Linie: So nicht in der Formulierung ...

   Eine Linie ist ein Punkt: Eher nein
   Eine Linie hat zwei Punkte: Kann man sagen // Schönheit des Satzes ist etwas einfach.
   Eine Linie besteht aus zwei Punkten: Etwas besser in der Formulierung.


b) Beziehung "hat-ein"  // "has-a" Relationsship

   // ein darf auch Plural sein // mehrere

   Ist die "hat-ein" Beziehung vorhanden, dass darf VERERBUNG NICHT angewendet werden.

   Hmm, was dann:

   Festlegung von Member Variablen.

   class Line
   {
       Point m_begin;
	   Point m_end;


=============================================================================

Nomenklatur bei der Vererbung:

class Rectangle
{
public:
    int m_x;       // linke, obere Ecke
    int m_y;
    int m_width;
    int m_height;
};

class ColoredRectangle : public Rectangle
{
public:
    int m_color;
};

Man spricht von 

== Basisklasse // Vaterklasse // Parent Class // Base class
== Kindklasse // child class // Abgeleitete Klasse // Derived Class

== Im Ganzen:

   - Eine Kindklasse ist eine Spezialisierung einer Vaterklasse.
   - Eine Vaterklasse stellt die Verallgemeinerung (Gerneralisierung) einer Kindklasse dar.

Durch die Vererbung werden alle Member-Variablen der Vaterklasse an die Kindklasse geerbt.


a) Wie gelangen Daten in eine Objekt: Durch Konstruktoren

b) Wie gelangen Daten in eine Basisklasse eines Objekts: Durch die Memberwise Initializer List ( : nach dem Konstruktor)

  2 Möglichgkeiten:

  i)  Man öffne die Basisklasse für Kindklassen: protected
  ii) (private): Ich benötige einen Basisklassenkonstruktor, den ich in der Memberwise Initializer List
      der abgeleiteten Klasse aufrufe.


============================================================================

Bemerkung:
---------

Regelfall: Eine Klasse und ihre Implementierung wird auf 2 Dateien AUFGETEILT.

Ausnahmen:

i) Kleine Klassen.
ii) Klassen, die ich nur in EINER Datei benötige.
iii) ZB kleine Klassen, um etwas zu demonstrieren.

Dann kann man eine Klasse auch in EINER Datei realisieren.

============================================================================

Virtuellen Methodenaufrufmechanismus
------------------------------------

    rp->print();    // Welches print  wird aufgerufen ????????????????????????????????????
                    // Das von Rectangle: WEIL rp ist vom Typ Rectangle (Compile / Übersetzungszeit)
                    // oder
                    // Das von ColoredRectangle: WEIL rp zeigt auf ein ColoredRectangle Objekt (Laufzeit / Runtime)


Virtuelle Methoden:

      Laufzeit entscheidet  // Late-Binding // Spätes Binden der Methodenadresse // Indirekter Methodenaufruf
	                        // Runtime // (Langsamere Ausführung)

Nicht-Virtuelle Methoden:

      Übersetzungszeit entscheidet // Early-Binding // Frühes Binden der Methodenadresse // Compile-Time // (Schnelle Ausführung)

virtual:

== Kennzeichnet Methoden in Basisklassen
== Diese haben eine Realisierung
== Diese Realisierung kann in Kindklassen geändert / erweitert werden.


i) virtual ist eine Einladung, dass diese Methode in der Kindklasse überschrieben wird. // muss aber nicht sein.

ii) non-virtual: Diese Methode wird an alle Kinkobjekte ver-erbt. Es gibt keinen Grund für eine Änderung dieser Methode.


=====================================================================================================

Dynamische Speicherverwaltung

new: reserviert Speicher

delete: Hebt die Reservierung auf.

Nochmal delete: Stürtz ab, weil dieser Zeiger so nicht mehr gültig ist.


=====================================================================================================

Array / Feld:

Eindimsionale Liste:

int zahlen[10];   Da liegen 10 int-Werte im Speicher DIREKT hintereinander.


Listen FESTER Länge: Array (Feld) // int zahlen[10]; 

Listen VARIABLER Länge: mit new/delete // std::vector 

Frage: Was geschieht mit dem Gesamt-Progamm, wenn einzelne delete's fehlen.

Aufgabe:

Kiosk:  Lose für eine Lotterie


 class LotteryTickets
 {
 private:
     int  m_count;     // Anzahl Tickets
     int* m_tickets;   // Diese Adresse // Pointer enthält das Ergebnis von new ...
 
 public:
     LotteryTickets() : m_count (0), m_tickets (NULL)
     {
         std::srand(  std::srand(  ( unsigned int)    std::time(0)); ); // use current time as seed for random generator
     }

	 LotteryTickets(int count) : m_count (count), m_tickets (NULL)
     {
         std::srand( std::srand(  ( unsigned int)  std::time(0)); ); // use current time as seed for random generator
     }

A) Im Konstruktor wird die Anzahl der Lose übergeben

B) In einer Methode 'buyTickets' werden m_count Lose angelegt (Feld m_tickets dynamisch mit new[m_count] anlegen .. )
   und belegt das Feld mit rand() Zahlen vor.

C) In einer Methode print werden die Lose ausgegeben.

D) In einer Methode release wird das dynamisch angelegte Feld freigegeben (delete[])

==========================================================================

== Musterlösung

== Destruktor

==========================================================================

A) Globale Variablen: Im gesamten Programm
------------------------------------------

i) Ist in EINER C-Datei zu definieren:

   int g_counter = 0;    // Ausserhalb von Funktionen // MIT Vorbelegungswert

ii) In jeder anderen Datei, in der ich diese Variable verwenden möchte:

   extern int g_counter; // eine extern Deklaration

   Tipp: Best Practice: Hierfür verwendet man eine Header-Datei

---------------------------------------------------------------------

B) Globale Variablen - aber nur in EINER Datei
----------------------------------------------

   Datei-global  // einfaches Modulkonzept

   SYNTAX: Wie Globale Variablen Definition , nur mit dem Schlüsselwort static

---------------------------------------------------------------------

C) Globale Variablen - aber nur in EINER Funktion / Methode
-----------------------------------------------------------

Auch hier kommt das Schlüsselwort static zum Einsatz:

static void main_GlobalVariable_WithinFunction()
{
    static int a = 1;

    a++;

    std::println("in main_GlobalVariable_WithinFunction: a = {}", a);
}

Ergibt das Sinn ???

Durchaus: Ich kann auf diese Weise erreichen, dass meine globale Variable geschützt ist.

-----------------------------------------------------------------

D) Lokale Variablen

Innerhalb von Funktionen / Methoden

-----------------------------------------------------------------

E) Dynamisch erzeugte Variablen

new / delete


======================================================================

namespace

Mit namespace kann man Konflikte mit doppelten Namen verhindern:

A) 

class Window {}    // problematisch ...dieser Name darf so NIRGENDS woanders verwendet werden

B)

namespace Project_A
{
    class Window {}  
}

namespace Project_B
{
    class Window {}  
}

void main()
{
    Window w;   // Fehler

	Project_A::Window w1;  // geht

	// und in derselben Funktion:

	Project_B::Window w2;  // geht
}

==============================================

Dynamische Speicherverwaltung:

A) new / delete

B) Klassen und Objekte:

   Hier können Zeiger von dynamisch reverviertem Speicher in den Membervariablen liegen / aufgehoben werden.

C) Wann / wie kommt es zum Aufruf von delete dieses revervierten Speichers?

i)  Explizite Methode, z. B. 'release'

ii) Konzept des Destruktors:

    Ähnlich zum Konzept eines Konstruktors:

	= Ist eine Spezialmethode (nur eine)
	= Wird aufgerufen, wenn ein Objekt aus dem Scope fällt / zerstört wird.
	= SYNTAX: Name der Methode: Wie Name der Klasse , mit führendem ~
	= Nochmal: Der Aufruf eines Destruktors wird AUTOMATISCH abgesetzt !!!


Fazit: Wenn in Klassen in den Member Variablen Zeiger vorhanden sind (durch new erzeugt),
   dann muss diese Klasse einen Destruktor haben mit passendem delete Aufruf.

Bemerkung:
   Es spricht nichts dagegen, auch eine explizite release-Methode hierfür zu implementieren.


======================================================================================

Rule of Three:

Kopie eines Objekts erstellen geht:

Alle Werte werden kopiert:  Zahlen (int), Adressen (Pointer)

Wenn Adressen vorhanden sind: PROBLEMATISCH // FALSCH

Lösung: Man muss den Kopier-Konstruktor EXPLIZiT implementieren.

Maschinenraum von C++
---------------------

Whyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy

Um bei delete - Aufrufen nicht denselben Zeiger 2-Mal freizugeben (double-delete / Absturz)
und um bei Objekte eine echte Kopie zu erhalten.

Siehe auch

Das copy-Modul (Standardweg)

Python bietet zwei Arten von Kopien über das copy-Modul:

Flache Kopie (copy.copy): Kopiert das Objekt, verweist bei verschachtelten Objekten aber weiterhin auf dieselben Referenzen.

Tiefe Kopie (copy.deepcopy): Kopiert das Objekt und alle darin enthaltenen Objekte rekursiv (erzeugt eine komplett unabhängige Kopie).


==================================================================
==================================================================

Template:

A) Man kann eine Klasse konkret - also mit bekannten, fixen Datentypen - definieren.

B) Man kann eine Klasse schablonenhaft - also mit Stellvertretern für Datentypen - definieren.

   Möchte man zu so einer Schablone eine konkrete Klasse haben, muss man dies bei Verwendung der Schablone angeben.



==================================================================
==================================================================

STL:   Standard Template Library

Bjarne Stroustrup:   C++                                      // C with Classes   // no templates

Alexander Alexandrowitsch Stepanow: Generische Programmierung // Programmieren mit Templates

                                    Ersteller der erste C++ Standard Template Library.

==================================================================

Erstes Beispiel:

Analogie:  Python List

C++:       std::vector

VORSICHT: Ein zentraler Unterschied:

Python:    [ 1, 2, 3, 4]   // [ 1, 1.5, "ABC" ]

C++:    Die Werte müssen vom SELBEN Typ sein.

======================================================================


std::vector:

a) Belegt intern Speicher

b) Beobachtung: Mehr, als tatsächlich benötigt wird.

c) getter capacity sagt, wieviel intern vorhanden ist

d) Bemkerkung: Visual C++:  Neue Kapazität = Alte Kapazität * 1.5  

e) WICHTIG: Man kann  / sollte die Kapazität von Anfang an einstellen.

==================================================================

STL:   Getting Started  

More STL: STL Konzepten